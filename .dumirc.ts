import { defineConfig } from 'dumi';

export default defineConfig({
  outputPath: 'docs-dist',
  base: '/react-fc-masonry/',
  publicPath: '/react-fc-masonry/',
  themeConfig: {
    name: 'react-fc-masonry',
    socialLinks: {
      github: 'https://github.com/bobowiki/react-virtual-list',
    },
  },
});
